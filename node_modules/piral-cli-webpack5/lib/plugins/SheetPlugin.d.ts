export default class SheetPlugin {
    private cssName;
    private entryName;
    private loaderPath;
    constructor(cssName: string, piletName: string, entryName: string);
    apply(compiler: any): void;
}
