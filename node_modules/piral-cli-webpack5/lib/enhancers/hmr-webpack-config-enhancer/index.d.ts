import { Configuration } from 'webpack';
export interface HmrWebpackPluginOptions {
    port: number;
}
export declare const hmrWebpackConfigEnhancer: (options: HmrWebpackPluginOptions) => (compilerOptions: Configuration) => Configuration;
