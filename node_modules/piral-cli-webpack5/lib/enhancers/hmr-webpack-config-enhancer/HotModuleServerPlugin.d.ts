import { Compiler, WebpackPluginInstance } from 'webpack';
export declare class HotModuleServerPlugin implements WebpackPluginInstance {
    private hmrPort;
    constructor(hmrPort: number);
    apply(compiler: Compiler): void;
}
